# Network Toolkit Environment Variables
# Copy this file to .env and fill in your actual credentials
# The .env file is automatically loaded when you run nw commands

# Default credentials for network devices
# These will be used when device-specific credentials are not provided
NT_DEFAULT_USER=admin
NT_DEFAULT_PASSWORD=your_secure_password_here

# Device-specific credentials (optional)
# Use pattern: NT_<DEVICE_NAME>_USER and NT_<DEVICE_NAME>_PASSWORD
# Device names should match those in devices.yml
NT_SW_ACC1_USER=admin
NT_SW_ACC1_PASSWORD=device_specific_password

NT_SW_ACC2_USER=admin
NT_SW_ACC2_PASSWORD=another_device_password

NT_SW_DIST1_USER=admin
NT_SW_DIST1_PASSWORD=distribution_switch_password

# Additional environment variables for configuration
# Optional: Override default directories
# NT_FIRMWARE_DIR=/path/to/firmware
# NT_BACKUP_DIR=/path/to/backups
# NT_LOGS_DIR=/path/to/logs
# NT_RESULTS_DIR=/path/to/results
